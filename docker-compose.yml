version: "3.8"

services:
  backend:
    container_name: node-backend
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
    command: npm start